// Import the User, Thought, and Reaction models from their respective files
const User = require('./User');
const Thought = require('./Thought');
const Reaction = require ('./Reaction');

// Export an object containing references to the User, Thought, and Reaction models
module.exports = { User, Thought, Reaction };
